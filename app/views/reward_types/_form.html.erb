<%= form_with(model: reward_type) do |form| %>
  <% if reward_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reward_type.errors.count, "error") %> prohibited this reward_type from being saved:</h2>

      <ul>
        <% reward_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
